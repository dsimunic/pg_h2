{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch erlang",
            "type": "erlang",
            "request": "launch",
            "cwd": "${workspaceRoot}/proxy",
            "preLaunchTask": "rebar3 compile",
            "arguments": "-eval 'application:start(pg_http)'"
        }
    ]
}